#include "main.h"
/**
 * puts2 - a function that reverses a string
 * @str: string input
 */
void puts2(char *str)
{
int i, j;
i = 0;
while (str[i] != '\0')
{
i++;
}
for (j = 0; j < i; j2++)
_putchar(str[j]);
_putchar('\n');
}
